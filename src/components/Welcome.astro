---
import Topbar from "@/components/Topbar.astro";
import { VERDICT_TONES } from "@/components/tonight/verdict-tones";

const { user } = Astro.locals;

const verdicts = [
  { label: "Go", dot: VERDICT_TONES.go.dot },
  { label: "Marginal", dot: VERDICT_TONES.marginal.dot },
  { label: "No-go", dot: VERDICT_TONES["no-go"].dot },
];
---

<div class="relative flex min-h-screen w-full flex-col overflow-hidden bg-background text-foreground">
  <!-- Star field -->
  <div
    class="pointer-events-none absolute inset-0"
    style="background-image: radial-gradient(circle, var(--star) 1px, transparent 1.5px), radial-gradient(circle, color-mix(in srgb, var(--star) 50%, transparent) 1px, transparent 1px), radial-gradient(circle, var(--star-accent) 1px, transparent 1px); background-size: 230px 230px, 130px 130px, 310px 310px; background-position: 0 0, 50px 80px, 140px 40px;"
  ></div>
  <!-- Faint glow along the horizon -->
  <div class="pointer-events-none absolute inset-x-0 bottom-0 h-1/3 bg-gradient-to-t from-surface via-surface/40 to-transparent"></div>

  <div class="relative z-10 mx-auto w-full max-w-5xl px-4 pt-4 sm:px-8 sm:pt-6">
    <Topbar />
  </div>

  <main class="relative z-10 mx-auto flex w-full max-w-3xl flex-1 flex-col items-center justify-center px-4 py-16 text-center sm:py-24">
    <p class="mb-4 text-xs font-semibold tracking-[0.18em] text-primary uppercase sm:tracking-[0.3em]">
      Messier 1 – 110 · your sky, your kit
    </p>
    <h1 class="mb-6 font-display text-5xl leading-tight font-semibold text-heading sm:text-7xl">Sidereus</h1>
    <p class="mb-3 font-display text-xl text-heading italic sm:text-2xl">Is tonight worth setting up for?</p>
    <p class="mb-8 max-w-xl text-base leading-relaxed text-muted-foreground sm:text-lg">
      A clear verdict for the night and a short, explained list of what to point your telescope at, with the eyepieces
      you already own.
    </p>

    <ul class="mb-10 flex flex-wrap justify-center gap-2 text-sm text-muted-foreground" aria-label="Night verdicts">
      {verdicts.map((v) => (
        <li class="inline-flex items-center gap-2 rounded-full border border-border bg-surface px-3 py-1">
          <span class:list={["size-2 rounded-full", v.dot]} aria-hidden="true" />
          {v.label}
        </li>
      ))}
    </ul>

    <div class="flex w-full flex-col gap-3 sm:w-auto sm:flex-row">
      {user ? (
        <a
          href="/dashboard"
          class="inline-flex items-center justify-center rounded-lg bg-primary px-6 py-3 font-semibold text-primary-foreground transition-colors hover:bg-primary/90"
        >
          Go to dashboard
        </a>
      ) : (
        <>
          <a
            href="/auth/signin"
            class="inline-flex items-center justify-center rounded-lg bg-primary px-6 py-3 font-semibold text-primary-foreground transition-colors hover:bg-primary/90"
          >
            Sign in
          </a>
          <a
            href="/auth/signup"
            class="inline-flex items-center justify-center rounded-lg border border-border px-6 py-3 font-semibold text-heading transition-colors hover:bg-accent"
          >
            Sign up
          </a>
        </>
      )}
    </div>
  </main>

  <footer class="relative z-10 px-4 pb-6 text-center text-xs text-faint">
    Named after Galileo's <em>Sidereus Nuncius</em>, 1610.
  </footer>
</div>
