---
// The starter's dashboard is retired: a returning user lands on Tonight. The path stays in PROTECTED_ROUTES,
// so an anonymous visitor is still sent to sign-in first.
return Astro.redirect("/tonight");
---
