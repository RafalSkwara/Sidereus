# Always use the public npm registry for this project, regardless of user- or
# machine-level npm config (e.g. a corporate Nexus mirror). Needed so
# `npm audit` and `npm install` resolve against npmjs.org.
registry=https://registry.npmjs.org/

# Fail installs on Node versions outside package.json "engines".
engine-strict=true
